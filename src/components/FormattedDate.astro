---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('id', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
